import 'package:flutter/material.dart';

const mobileBackgroundColor = Colors.white;
const webBackGroundColor = Colors.white;
const primaryColor = Colors.black;
const secondaryColor = Colors.grey;
const themeColor = Colors.deepOrange;
const appBarColor = Color.fromARGB(255, 240, 240, 240);

const messageSendColor = Color.fromARGB(255, 201, 87, 53);
const messageReceiveColor = Color.fromARGB(255, 212, 212, 212);

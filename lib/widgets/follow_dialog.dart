import 'package:flutter/material.dart';

class FollowDialog extends StatelessWidget {
  const FollowDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Dialog();
  }
}
